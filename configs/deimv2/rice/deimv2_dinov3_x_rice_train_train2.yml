# -------------------------
# Runtime / global
# -------------------------
print_freq: 500
output_dir: ./outputs/rice_train/deimv2_dinov3_x_rice_train_train2

sync_bn: True
find_unused_parameters: True

use_amp: True
scaler:
  type: GradScaler
  enabled: True

use_ema: True
ema:
  type: ModelEMA
  decay: 0.9999
  warmups: 100
  start: 0

epoches: 150
clip_max_norm: 0.1

# -------------------------
# Dataset / evaluator
# -------------------------
task: detection

evaluator:
  type: CocoEvaluator
  iou_types: ['bbox']

num_classes: 1
remap_mscoco_category: False

# -------------------------
# Train / Val dataloaders (CocoDetection)
# -------------------------
train_dataloader:
  type: DataLoader
  shuffle: True
  total_batch_size: 16
  num_workers: 16
  drop_last: True

  dataset:
    type: CocoDetection
    img_folder: /tangquan/DatasetT/RiceHybrids/morphological_difference_labels_phone/coco_train/images/train
    ann_file: /tangquan/DatasetT/RiceHybrids/morphological_difference_labels_phone/coco_train/annotations/annotations_train.json
    return_masks: False
    transforms:
      type: Compose
      ops:
        - {type: Mosaic, output_size: 640, rotation_range: 10, translation_range: [0.1,0.1], scaling_range: [0.5,1.5],
           probability: 1.0, fill_value: 0, use_cache: True, max_cached_images: 50, random_pop: True}
        - {type: RandomPhotometricDistort, p: 0.5}
        - {type: RandomZoomOut, fill: 0}
        - {type: RandomIoUCrop, p: 0.8}
        - {type: SanitizeBoundingBoxes, min_size: 1}
        - {type: RandomHorizontalFlip}
        - {type: Resize, size: [640, 640]}
        - {type: SanitizeBoundingBoxes, min_size: 1}
        - {type: ConvertPILImage, dtype: 'float32', scale: True}
        - {type: Normalize, mean: [0.485,0.456,0.406], std: [0.229,0.224,0.225]}
        - {type: ConvertBoxes, fmt: 'cxcywh', normalize: True}
      policy:
        name: stop_epoch
        epoch: [20, 79, 120]
        ops: ['Mosaic', 'RandomPhotometricDistort', 'RandomZoomOut', 'RandomIoUCrop']
      mosaic_prob: 0.5

  collate_fn:
    type: BatchImageCollateFunction
    # Mixup options
    mixup_prob: 0.5
    mixup_epochs: [0, 100]
    stop_epoch: 110    # epoch in [72, ~) stop `multiscales`
    # CopyBlend options
    copyblend_prob: 0.5
    copyblend_epochs: [0, 100]
    area_threshold: 100
    num_objects: 3
    with_expand: True
    expand_ratios: [0.1, 0.25]

    ema_restart_decay: 0.9999
    base_size_repeat: 3

val_dataloader:
  type: DataLoader
  shuffle: False
  total_batch_size: 16
  num_workers: 2
  drop_last: False
  collate_fn:
    type: BatchImageCollateFunction

  dataset:
    type: CocoDetection
    img_folder: /tangquan/DatasetT/RiceHybrids/morphological_difference_labels_phone/coco_train/images/val
    ann_file: /tangquan/DatasetT/RiceHybrids/morphological_difference_labels_phone/coco_train/annotations/annotations_val.json
#测试集
#    img_folder: /tangquan/DatasetT/RiceHybrids/morphological_difference_labels_phone/coco_train/images/val/test
#    ann_file: /tangquan/DatasetT/RiceHybrids/morphological_difference_labels_phone/coco_train/annotations/annotations_test.json
    return_masks: False
    transforms:
      type: Compose
      ops:
        - {type: Resize, size: [640, 640]}
        - {type: ConvertPILImage, dtype: 'float32', scale: True}
        - {type: Normalize, mean: [0.485,0.456,0.406], std: [0.229,0.224,0.225]}

# -------------------------
# Model / DEIMv2 (use DINOv3STAs backbone)
# -------------------------
model: DEIM
criterion: DEIMCriterion
postprocessor: PostProcessor

use_focal_loss: True
eval_spatial_size: [640, 640]
checkpoint_freq: 5

DEIM:
  backbone: DINOv3STAs
  encoder: HybridEncoder
  decoder: DEIMTransformer

DINOv3STAs:
  name: dinov3_vits16plus
  weights_path: ./ckpts/dinov3_vits16plus_pretrain_lvd1689m-4057cbaa.pth
  interaction_indexes: [5, 8, 11]
  finetune: True
  conv_inplane: 64
  hidden_dim: 256

HybridEncoder:
  in_channels: [256, 256, 256]
  feat_strides: [8, 16, 32]

  hidden_dim: 256
  use_encoder_idx: [2]
  num_encoder_layers: 1
  nhead: 8
  dim_feedforward: 1024
  dropout: 0.
  enc_act: 'gelu'

  expansion: 1.25
  depth_mult: 1.37
  act: 'silu'

  version: deim
  csp_type: csp2
  fuse_op: sum

DEIMTransformer:
  feat_channels: [256, 256, 256]
  feat_strides: [8, 16, 32]
  hidden_dim: 256
  num_levels: 3

  num_layers: 6
  eval_idx: -1
  num_queries: 300

  num_denoising: 100
  label_noise_ratio: 0.5
  box_noise_scale: 1.0

  reg_max: 32
  reg_scale: 4
  layer_scale: 1

  num_points: [3, 6, 3]
  cross_attn_method: default
  query_select_method: default

  activation: silu
  mlp_act: silu
  dim_feedforward: 2048

PostProcessor:
  num_top_queries: 300

# -------------------------
# Criterion / Loss
# -------------------------
DEIMCriterion:
  weight_dict: {loss_mal: 2.5, loss_bbox: 5, loss_giou: 4, loss_fgl: 0.15, loss_ddf: 1.5}
  losses: ['mal', 'boxes', 'local']
  gamma: 1.5
  alpha: 0.75
  reg_max: 32

  matcher:
    type: HungarianMatcher
    weight_dict: {cost_class: 4, cost_bbox: 5, cost_giou: 4}
    alpha: 0.25
    gamma: 2.0
    change_matcher: True
    iou_order_alpha: 4.0
    matcher_change_epoch: 45

# -------------------------
# Optimizer (param groups + initial_lr)
# -------------------------
optimizer:
  type: AdamW
  params:
    -
      # except norm/bn/bias in self.dinov3
      params: '^(?=.*.dinov3)(?!.*(?:norm|bn|bias)).*$'
      lr: 0.00002
    -
      # including norm/bn/bias in self.dinov3
      params: '^(?=.*.dinov3)(?=.*(?:norm|bn|bias)).*$'
      lr: 0.00001
      weight_decay: 0.
    -
      # including norm/bn/bias except for the self.dinov3
      params: '^(?=.*(?:sta|encoder|decoder))(?=.*(?:norm|bn|bias)).*$'
      weight_decay: 0.

  lr: 0.0002
  betas: [0.9, 0.999]
  weight_decay: 0.0005


# -------------------------
# LR Scheduler (FlatCosine)
# -------------------------
lr_scheduler:
  type: MultiStepLR
  milestones: [500]
  gamma: 0.1

lr_warmup_scheduler:
  type: LinearWarmup
  warmup_duration: 500


#lrsheduler: flatcosine
#lr_gamma: 1
#warmup_iter: 500
#flat_epoch: 50
#no_aug_epoch: 0
lrsheduler: flatcosine
lr_gamma: 0.1
warmup_iter: 1000
flat_epoch: 79    # 4 + epoch // 2, e.g., 40 = 4 + 72 / 2
no_aug_epoch: 30

# -------------------------
# Misc
# -------------------------
# any extra project-specific settings can be added below
